# Classic session CSRF (no Stimulus/importmap required for this demo).
# The Flex recipe enables cookie-based "stateless" tokens that need
# data-controller="csrf-protection" JS — not wired in this minimal demo.
framework:
    csrf_protection: true
    form:
        csrf_protection:
            enabled: true
