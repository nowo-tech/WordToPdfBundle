WordToPdfBundle is installed.

System requirement: install LibreOffice Writer on the host/container, e.g.:

  Debian/Ubuntu:  sudo apt-get install -y libreoffice-writer
  Alpine:         apk add libreoffice

Then run:  php bin/console nowo:word-to-pdf:check
